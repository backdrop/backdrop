type = module
name = CKEditor 4 (Deprecated)
description = WYSIWYG editing for rich text fields using CKEditor 4.
package = User Interface
tags[] = Filters
tags[] = Formats
tags[] = Text Editors
version = BACKDROP_VERSION
backdrop = 1.x

configure = admin/config/content/formats

; This module is deprecated, so hide it from new installs. Existing sites that
; have CKEditor 4 module enabled will continue to see it in the modules list,
; but once it is disabled, it will be hidden. Temporarily removing this line
; will allow the module to be enabled if needed.
hidden = TRUE
