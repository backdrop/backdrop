name = Code Sign UI
description = Provides an administrative interface for creating and managing Code Sign signing profiles.
package = Security
version = BACKDROP_VERSION
type = module
backdrop = 1.x
dependencies[] = code_sign

; The UI is used rarely and is enabled by Project Code Sign when needed.
;hidden = true
