[TextFieldTestCase]
name = Text field
description = Test the creation of text fields.
group = Field types
file = text.test

[TextSummaryTestCase]
name = Text summary
description = Test text_summary() with different strings and lengths.
group = Field types
file = text.test

[TrimOnWordBoundaryTestCase]
name = Trim on word boundary
description = Test trimming strings on word boundary.
group = Field types
file = text.test

[TextTranslationTestCase]
name = Text translation
description = Check if the text field is correctly prepared for translation.
group = Field types
file = text.test
